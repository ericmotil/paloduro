# LE MAB

## Getting Started

* Clone down
* Install dependancies:
  * `npm install` or `yarn`

## Relevant Scripts

* Local dev
  * `npm start` or `yarn dev`
* Deploy gh-pages preview
  * `npm run deploy` or `yarn deploy`
